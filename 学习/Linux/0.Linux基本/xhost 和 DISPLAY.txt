xhost 和 DISPLAY

由於 UNIX通常配備 X 視窗，所以請先進入 X 視窗環境。您必須要知道以下資料，才能使用遠端的 X： 

目的地電腦的網域名稱或是 IP 位址 
您目前所使用的電腦的網域名稱或是 IP 位址 
設定 xhost 與 DISPLAY 環境參數
一、設定xhost 
請先在本機下這段指令 
xhost +HostName 
HostName 是遠端的伺服器。 

在 HostName 前加一個加號，代表允許接受由 HostName 地方所傳來的 X 繪圖指令。 
倘若未加 HostName，則代表可以讓所有地方的 X 繪圖指令都能進來，這是很不安全的情況。 
若將上述之加號改成減號，則代表拒絕由 HostName 傳來的 X 繪圖指令。 

二、使用遠程簽入程式 
使用 UNIX 提供的遠程簽入程式，如 telnet、ssh 等，登入遠端伺服器。遠程簽入程式的相關使用請參閱 第三講。 

三、設定DISPLAY參數 
登入遠端伺服器後請下這段指令 
setenv DISPLAY HostName:0.0 
在這裡 HostName 指的是目前近端所使用的電腦的網域名稱或是 IP 位址。 
如下圖所示，HostName 是我目前近端這台電腦，在 HostName 後加一個冒號，再加上 0.0。第一個數字 0，代表我現在近端這台電腦的第一張顯示卡，如果有多張顯示卡，則按照順序加上去。第二個數字 0，代表我現在近端這台電腦的這張顯示卡的第一個螢幕；同理，如果同張顯示卡可以接多個螢幕，其數字依序往上加。 

然後就大功告成了。測試遠端 X 程式，


-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------‘


Linux / Unix的命令： xhost的

Linux上的“网络 “

相比之下traditio NAL“个人”电脑的典型使用，在Linux / Unix环境下，“在网络上”一直是常态，这也解释了UNIX和Linux操作系统的强大的网络功能。 您可以轻松地连接到其他计算机，并在网络上运行的图形用户界面。 在这篇文章中，我们描述了一个简单的方法，在计算机网络上的任何没有做你的工作，离开座位。

关键的命令是：xhost的 -为X服务器的访问控制程序xhost的程序用来添加和删除主机（计算机）名称或用户名称，允许连接到X服务器的机器和用户列表。 这提供了隐私控制和安全的基本形式 。

概述：让你坐在“本地主机”和你的电脑要连接到“远程主机”的号召计算机。 首先使用xhost的指定你想给权限（X服务器）本地主机连接的计算机（S）。 然后你使用telnet连接到远程主机。 接下来，您在远程主机上设置DISPLAY变量。 您要设置DISPLAY变量到本地主机。 现在，当您启动远程主机上的一个程序，其GUI将显示在本地主机上（而不是在远程主机上 ）。

一个典型的应用实例

假设本地主机的IP地址是128.100.2.16和远程主机的IP地址为17.200.10.5。 根据你在网络上，你也可以使用的计算机名称而不是IP地址（域名）。 

第1步。 在本地主机上

在命令行键入以下内容：

％xhost的+ 17.200.10.5 
<按回车键>

第2步。 登录到远程主机

％的telnet 17.200.10.5

第3步。 在远程主机（通过telnet连接）

指示远程主机显示在本地主机上键入窗口：

％的setenv显示128.100.2.16:0.0
export DISPLAY=128.100.2.16:0.0
//本地export DISPLAY=:0.0

在命令行。 （而不是SETENV您可能不得不使用某些炮弹的出口。 ）

<按回车键>

第4步。 现在，您可以从远程主机运行的软件 。

例如：当您键入％远程主机上的 xterm，你应该看到本地主机上的一个xterm窗口 。 

第5步。 完成后

您应该删除远程主机从您的访问控制列表如下 。 本地主机上的类型： 

％xhost的- 17.200.10.5 
<按回车键>

快速参考- xhost的

xhost的+主机名 ：X服务器访问控制列表中添加主机名。
xhost的-主机名 ：从X服务器访问控制列表中删除的主机名。
xhost的+：关闭acccess控制打开（所有远程主机将有机会获得X服务器）
xhost的- ：打开访问控制上。
重要事项 ： 使用 man 命令（男子）看到您的特定计算机上使用的命令是如何 。


